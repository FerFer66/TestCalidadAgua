1.- Para ejecutar este programa se debe acceder a la carpeta de python (previamente debe haber instalado python) en el cmd (Usualmente está en C:\Python21) e instalar las siguientes librerias. Para esto debe ejecutar el siguiente codigo desde una terminal.
python -m pip install --upgrade pip
python -m pip install pandas
python -m pip install numpy
python -m pip install matplotlib

